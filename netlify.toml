[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback — let React Router handle all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
